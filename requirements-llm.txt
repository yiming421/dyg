# Optional PEFT / Hugging Face LLM link prediction support.
-r requirements.txt
peft==0.10.0
datasets==2.18.0
accelerate==1.12.0
# vLLM and TabICL are optional backends with separate runtime requirements.
# Install them only for the corresponding entrypoint in experiments/README.md.
